# GEMINI.md - Project AI Rules

## File Access Rules

- **Git-Ignored Files**: You ARE allowed to read files listed in `.gitignore` for context and debugging purposes.
- **Tool Usage**: When searching for files, you may need to use flags like `-u` or `--no-ignore` in `grep_search` or `find_by_name` (if supported) to find files that are in `.gitignore`. If not supported, you can still access these files directly via `view_file` if you know their path.

## Specific Restrictions

- [Insert specific project-level restrictions here if any]
